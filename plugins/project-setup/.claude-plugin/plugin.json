{
  "name": "project-setup",
  "description": "프로젝트 계획서 기반 Claude Code 설정 자동 생성. 계획서 작성(/project-setup:plan) → 고도화(/project-setup:refine) → 셋업(/project-setup:setup) 3단계 워크플로우.",
  "author": {
    "name": "eyecool7"
  },
  "commands": "./commands/",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo ${CLAUDE_PLUGIN_ROOT} > /tmp/.project-setup-root"
          }
        ]
      }
    ]
  }
}